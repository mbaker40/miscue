// MISCUE v2 boot. M0 scaffold stub — replaced when the render/camera packet lands.
const app = document.getElementById('app')!;
app.innerHTML = `<div style="position:fixed;inset:0;display:flex;align-items:center;justify-content:center;
  flex-direction:column;gap:12px;text-align:center;padding:24px;">
  <div style="font-family:Bungee,cursive;font-size:clamp(28px,8vw,54px);color:var(--neon);
    text-shadow:0 0 30px rgba(46,242,197,.8);">MISCUE v2</div>
  <div style="color:#8fb8ab;font-size:14px;">the long tables are being built…</div>
</div>`;
export {};
